# Universal I2C validation receiver

This folder contains the Eyes-side receiver used by every numbered test in
`BU22_Brain/tests/i2c_validation/`. It deliberately retains all raw commands
that were proven during breadboard development. It is test firmware, not the
final BU-22 Eyes protocol or production firmware.
